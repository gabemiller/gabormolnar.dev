language: node_js
node_js:
  - "12.13.0"
before_install:
  - npm i -g cross-env
before_script:
  - npm install
script:
  - npm run build
deploy:
  local-dir: dist
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN
  keep-history: true
  on:
    branch: master